@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = "_LayoutLogin";
}

<div class="card shadow-sm">
    <div class="card-header text-center bg-danger text-white">
        <h4>Acceso Denegado</h4>
    </div>
    <div class="card-body text-center">
        <p class="text-danger">No tiene los permisos necesarios para acceder a esta página.</p>
        <p>Por favor, contacte al administrador del sistema si cree que esto es un error.</p>
        <a asp-action="Login" asp-controller="Account" class="btn btn-primary mt-3">Volver al Inicio de Sesión</a>
    </div>
</div>
