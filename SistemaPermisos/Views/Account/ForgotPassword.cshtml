@model SistemaPermisos.ViewModels.ForgotPasswordViewModel

@{
    ViewData["Title"] = "Recuperar Contraseña";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header text-center">
                <img src="~/images/logo.png" alt="CTPMN Logo" style="height: 60px; margin-bottom: 10px;" />
                <h2>@ViewData["Title"]</h2>
                <p class="text-muted">Ingrese su correo electrónico para recibir instrucciones</p>
            </div>
            <div class="card-body">
                <form asp-action="ForgotPassword">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group mb-3">
                        <label asp-for="Correo" class="control-label"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                            <input asp-for="Correo" class="form-control" placeholder="ejemplo@escuela.edu" />
                        </div>
                        <span asp-validation-for="Correo" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3 text-center">
                        <button type="submit" class="btn btn-primary btn-lg w-100">
                            <i class="fas fa-paper-plane"></i> Enviar Instrucciones
                        </button>
                    </div>
                </form>
                <div class="text-center mt-3">
                    <a asp-action="Login" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Volver al Inicio de Sesión
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
